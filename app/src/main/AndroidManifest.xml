<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cfreesespuffs.github.giftswapper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Giftswapper">
        <activity android:name=".PendingPartyFragment"></activity>
        <activity android:name=".Activities.SignupConfirmation" />
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".PostParty" />
        <activity android:name=".CurrentParty" />
        <activity android:name=".PendingPage" />
        <activity android:name=".InvitedPartyPage" />
        <activity android:name=".Activities.HostParty" />
        <activity android:name=".UserProfile" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>